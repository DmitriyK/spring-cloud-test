spring:
  cloud:
    config:
      name: service1
      uri: http://localhost:8888
      profile: development
  #cloud:
    #config:
      #uri: http://localhost:8080
      #discovery:
        #enabled: true
        #service-id: config-server
      #label: master
  #profiles:
    #active: development
#server:
  #port: 8081